# [2026-09-19 20:05:00] 历史清理失败后队列不再继续

- **问题描述**: 运行收尾时 cleanupOldRuns 抛错会跳过 scheduleNext，已排队任务卡住。
- **修改方案**: 历史清理失败只记录诊断，广播与调度仍执行；补「清理失败也继续队列」回归。
- **验证状态**: 已通过
- **说明**: `bun test app/server/tests/runner-lifecycle.test.ts` 与 `bun run check` 通过。

# [2026-09-19 19:50:00] Windows 打包产物静态资源 404

- **问题描述**: CI 在 windows-2025 启动实际产物后，首页能打开，但 GET /assets/*.js 返回 404。
- **修改方案**: 生成内嵌资源时把 Glob 相对路径统一成 URL 斜杠，避免 Windows 反斜杠键与 HTTP 路径对不上。
- **验证状态**: 已通过
- **说明**: 本地补了反斜杠路径回归；`ff19d5b` 的 windows-2025 构建冒烟已通过。

# [2026-09-19 19:17:02] 补齐质量门禁并修复覆盖范围与测试隔离

- **问题描述**: 原覆盖率漏掉未加载源码，CRAP 会漏算函数；Bun 裸目录筛选重复执行前端测试，覆盖率目录忽略规则还误伤检查器源码。本机模型配置掩盖了 CI 示例配置失败。
- **修改方案**: AST 计算复杂度，完整源码预插桩，逐文件强制行与函数全覆盖；前后端使用显式相对目录各执行一次，共用 LCOV。只忽略根报告目录并纳入质量源码与修复记录；测试注入受控模型配置。插桩结束恢复 Babel 修改的全局错误堆栈设置，合并报告前克隆基线，防止重复累计命中次数；通知测试只读取当前有效通知。
- **验证状态**: 已通过
- **说明**: 使用 CI 示例配置运行 774 项测试，143 个手写源文件行 3717/3717、函数 1287/1287；CRAP 无超限。真实 Bun 发现回归验证测试各执行一次；格式、lint、类型检查和 actionlint 通过。变异测试 1700 项（1204 检出、1 超时、495 存活），70.88%，耗时 8 分 57 秒；据此将失败门槛从 50% 提升至 70%。跨平台执行结果仍以对应 CI 为准。

# [2026-09-19 19:17:02] 补齐异常路径时修复执行清理与输入错误

- **问题描述**: 浏览器同步或异步关闭失败可能留下运行状态；变量接口接收数组会清空变量；表单自动校验网络失败会误报通过。YAML 简化后普通对象的继承属性可能成为动作。冒烟脚本可能把分块输出中的半截端口当作完整端口。
- **修改方案**: 清理资源前移除引用并保证状态释放、队列继续；拒绝变量数组；展示校验失败原因；动作解析使用自身属性判断。冒烟等待完整启动日志行，兼容 LF/CRLF；使用独立数据、临时端口与本地页面。
- **验证状态**: 已通过
- **说明**: Runner 生命周期、Hono 路由、真实 React 表单、YAML 原型动作与端口分块回归均通过；前端保留保存前重新校验。

# [2026-09-19 19:17:02] 修复构建工具与系统适配的错误路径

- **问题描述**: Windows 端口清理依赖不可用的外部 sleep 命令；下载失败可能留下临时文件；Windows 构建需明确准备解压工具。
- **修改方案**: 使用 Bun.sleepSync，下载流程始终回收临时文件；构建矩阵固定系统与 Bun，Windows 使用 Git Bash 并检测 unzip；交付冒烟检查本机产物、附属工具和静态资源路由。
- **验证状态**: 已通过
- **说明**: 系统适配、下载与构建命令的成功及失败路径测试通过；使用 CI 示例模型配置，本机 macOS arm64 的 bun run build:exe、实际产物 BUILD_SMOKE_PASS 和网页 E2E_SMOKE_PASS 均通过。生成资源已恢复，开发模型配置保持不变；Linux/Windows 实际运行由 CI 验证。

# [2026-09-19 18:29:26] 复杂度扫描漏掉函数并截断正则后的函数体

- **问题描述**: 手写解析器漏掉 ai-error 与 Android 部分函数，将 parseWebTarget 截断，导致 CRAP 报告错误通过。
- **修改方案**: 使用 Babel 的 TypeScript/JSX 语法树识别实际函数和决策点；嵌套函数独立计分，类型签名不计入，语法错误直接失败。
- **验证状态**: 已通过
- **说明**: 新增正则、类型签名、TSX、方法、循环和语法错误回归；全量 380 项测试通过，format、lint、typecheck 通过。应用源码识别 1164 个函数，真实发现 detectAdbPath、parseWebTarget 两项超标，后续阶段继续修复，不再报告虚假通过。

# [2026-09-13] 步骤组换了页面后自由指令仍直接跳到其他网站搜索

- **问题描述**: 运行 #9 步骤组 2 已配置 `https://www.baidu.com/`，执行前截图也是百度首页，但自由指令「搜索google」调用 Midscene 的 Navigate 直接打开 Google，被验证码拦截后失败。
- **修改方案**: 网页 Agent 仍保留同站 Navigate（如 Bing 搜 baidu），但跨站跳转会失败；并把已打开页面写入 `aiActContext`，让搜索走当前页搜索框。
- **验证状态**: 已通过
- **说明**: `bun test` 347 通过；运行 #11 步骤组 2 执行前已是百度首页，搜索留在 baidu.com（验证码页 `wappass.baidu.com`），不再跳到 Google。

# [2026-09-12] API Key 缺失或无效时进程退出

- **问题描述**: 未配置 API Key（含中文占位符）或 Key 错误时，Bun 把未处理的 Promise 拒绝当成致命错误退出；启动恢复队列时 `loadModels()` 抛错也会把进程打死。运行记录还会把鉴权失败伪装成「找不到元素」。
- **修改方案**: 注册 unhandledRejection/uncaughtException 监听并继续运行；配置解析拒绝非 ASCII Key；启动/队列失败写成运行错误；鉴权失败输出明确中文提示。
- **验证状态**: 已通过
- **说明**: `bun test` 317 通过；`bun run crap` 968 个函数、0 不通过；lint / format / typecheck 通过。

# [2026-09-12] 新建队列误用列表第一项当 id

- **问题描述**: 新建队列丢掉 createQueue 返回的 id，再按列表第一项更新，并发时会写到别人的队列。
- **修改方案**: 用创建接口返回的 id 调用 updateQueue，并补 WS 归并测试。
- **验证状态**: 已通过
- **说明**: `bun test` 304 通过；`bun run crap` 962 个函数、0 不通过；lint / format / typecheck 通过。

# [2026-09-12] CRAP 检查器漏计对象返回类型和泛型函数

- **问题描述**: `skipReturnType` 把返回类型里的 `{` 当成函数体；`function foo<T>` 因名字后是 `<` 整段不进检查。`parseBboxJson`、`checkModelVision`、`request`、`activatePageSession` 被静默豁免。
- **修改方案**: 按深度跳过返回类型中的对象/泛型；函数名后跳过 `<...>`。拆分新计入且超门槛的函数并补测试。
- **验证状态**: 已通过
- **说明**: `bun test`、`bun run crap`（961 个函数、0 不通过）、lint、format、typecheck 本地通过。

# [2026-09-14 00:03:00] 模型设置功能的构建验证限制

- **问题描述**: 直接使用 Bun 构建完整前端时，现有 Tailwind 指令出现警告，代码编辑器依赖在页面加载时触发 `tag.modified` 未定义异常。
- **修改方案**: 未改动原有构建链；使用 Bun 单独构建设置页并加载现有开发服务生成的样式，在独立数据目录验证配置编辑、保存、刷新及错误提示。
- **验证状态**: 当前无法验证
- **说明**: 完整前端的 Bun 构建兼容性问题仍存在；设置页独立构建、浏览器交互、全量单元测试、类型与静态检查通过。未调用项目中依赖 Vite 的构建脚本，以遵循本项目禁止使用 Vite 的指令。

# [2026-09-14 00:30:00] 修复模型设置的 PR 质量门槛

- **问题描述**: 创建 PR 前本地补跑 CRAP，发现新增配置路由、模型表单与系列选择字段存在覆盖率或复杂度超限。
- **修改方案**: 拆出可注入存储依赖的配置路由，补充保存、默认模型、失败分支测试；补充系列字段与加载状态渲染测试；拆分 URL 校验和加载提示；JSON 解析异常使用通用错误，避免回显敏感片段。
- **验证状态**: 已通过
- **说明**: 本地 368 项测试通过，CRAP 1180 个函数全部达标；类型、lint 和格式检查通过。

# [2026-09-14 00:56:00] 失败原因被任务内容覆盖

- **问题描述**: AI 步骤抛出英文或中英混合错误时，后端丢弃实际异常，按动作类型和任务提示词拼出失败原因；读取历史记录时也重复替换。
- **修改方案**: 删除按语言占比、动作类型和任务内容生成错误的逻辑，步骤记录与历史读取统一使用异常格式化；保留已有 API Key 提示及长度限制。
- **验证状态**: 已通过
- **说明**: 374 项单元测试、lint、格式、类型、前端构建和 CRAP 检查通过；变异测试 59.10%，达到 50% 门槛。独立数据目录分别使用 bun run dev、bun run dev:server、bun run start 验证模型服务实际报错、演示成功和失败即停，实时事件与历史接口错误一致。旧记录若已覆盖并保存，无法从任务内容还原原始异常，需重新运行。
- **真实模型补充验证**: 使用本地 Ollama 配置中的 Gemma 4 与 Kimi K2.7，通过 bun run dev 启动，均成功点击登录按钮，并在不存在的页面内容断言处失败；失败记录保留模型解释，没有用任务内容覆盖。
